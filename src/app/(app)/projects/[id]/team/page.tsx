'use client';

export default function TeamPage() {
    return (
        <div className="max-w-2xl mx-auto">
            <div className="card p-8 text-center">
                <h1 className="text-xl font-bold mb-2" style={{ color: 'var(--color-slate-900)' }}>
                    Team feature is disabled
                </h1>
                <p className="text-sm" style={{ color: 'var(--color-slate-500)' }}>
                    This MVP uses organization users directly and does not manage project team members.
                </p>
            </div>
        </div>
    );
}
